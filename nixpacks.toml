[phases.setup]
aptPkgs = ["fonts-noto-cjk", "ghostscript", "libpango-1.0-0", "libpangocairo-1.0-0", "libgdk-pixbuf2.0-0", "libffi-dev", "libcairo2"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
